services: 
  oracle-db:
    image: gvenzl/oracle-free
    environment:
        ORACLE_PASSWORD: ${ORACLE_PASSWORD}
        APP_USER: ${APP_USER}  
        APP_USER_PASSWORD: ${APP_USER_PASSWORD}
    ports:
      - 1521:1521
      - 5500:5500