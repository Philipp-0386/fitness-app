# Automatically merged by `docker compose` when no -f is given
services:
  db:
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
  backend:
    ports:
      - "${BACKEND_PORT:-8080}:8080"
  frontend:
    ports:
      - "${FRONTEND_PORT:-3000}:3000"