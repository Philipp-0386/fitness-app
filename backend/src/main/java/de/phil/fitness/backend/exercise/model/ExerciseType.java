package de.phil.fitness.backend.exercise.model;

/**
 * Kind of exercise, mirroring the check constraint on {@code exercise.exercise_type}.
 */
public enum ExerciseType {
    STRENGTH,
    CARDIO,
    MOBILITY
}
