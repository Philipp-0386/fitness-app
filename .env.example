# Copy to .env and fill in. Used by the root compose.yaml for all services.

# Database
POSTGRES_DB=x
POSTGRES_USER=y
POSTGRES_PASSWORD=z

# Backend
JWT_SECRET=at-least-64-bytes-for-HS512
API_DOCS_ENABLED=false # for  Swagger UI and /v3/api-docs

# Host ports (optional, defaults shown)
POSTGRES_PORT=5432
BACKEND_PORT=8080
FRONTEND_PORT=3000
